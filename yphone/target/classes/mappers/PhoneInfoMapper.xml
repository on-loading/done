<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yphone.mappers.PhoneInfoMapper" >
    <resultMap id="partResultMap" type="com.yphone.model.nochange.PhoneInfo">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="phone_id" jdbcType="BIGINT" property="phoneId" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="price" jdbcType="DECIMAL" property="price" />
        <result column="capacity" jdbcType="TINYINT" property="capacity" />
        <result column="RAM" jdbcType="TINYINT" property="ram" />
        <result column="url" jdbcType="VARCHAR" property="url" />
    </resultMap>
    <select id="getType" resultType="java.lang.String">
        select type from phone_info GROUP BY type
    </select>
    <select id="getNamesByType" resultType="java.lang.String" parameterType="java.lang.String">
        select name from phone_info where type=#{type}
    </select>

    <select id="getPhoneByName" resultType="com.yphone.model.nochange.PhoneInfo" parameterType="java.lang.String">
        SELECT * from phone_info where name =#{name}
    </select>

    <select id="selectByType" resultMap="partResultMap" parameterType="java.lang.String">
        SELECT phone_id,name,price,capacity,RAM,description,url from phone_info where type=#{type}
    </select>

    <select id="selectByMinID" resultType="com.yphone.model.nochange.PhoneInfo">
        select * from phone_info where phone_id in(select min(phone_id) from phone_info);
    </select>

    <select id="getNameByID" resultType="java.lang.String" parameterType="java.lang.Long">
        SELECT name from phone_info WHERE phone_id=#{ID}
    </select>
</mapper>